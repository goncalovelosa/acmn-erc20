# Copy this to .env and fill in values as needed
# cp .env.example .env

# RPC endpoint URL for the target network (Anvil, testnet, mainnet, etc.)
RPC_URL=http://127.0.0.1:8545

# Private key for the deployer (hex string without 0x or decimal). Use an Anvil key for local.
# DO NOT commit your real private keys.
PRIVATE_KEY=

# ERC20 metadata
TOKEN_NAME="My Token"
TOKEN_SYMBOL=MYT
TOKEN_DECIMALS=18

# Supply configuration
# Fill these with RAW INTEGER values (no decimals, no expressions), in base units according to TOKEN_DECIMALS.
# Example for 18 decimals:
# - 1,000,000 tokens -> 1000000000000000000000000
# -   100,000 tokens -> 100000000000000000000000
TOKEN_CAP=
TOKEN_INITIAL_SUPPLY=
